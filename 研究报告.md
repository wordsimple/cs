# OTG 支付系统自动化测试研究报告

## 一、系统基础信息

### 1. Base URL
```
系统登录URL: https://operator1.otgpaytest.com/#/login
系统基础URL: https://operator1.otgpaytest.com/#
```

### 2. 测试账户凭证
```
用户名: ceshi
密码: qw123456
TOTP密钥: 6gnkpcdu22be5pahlbvfvffmnxcqzjzv (TOTP/双因素认证)
```

### 3. 架构概览
- 前端框架: Vue.js + Element UI
- 构建工具: 支持代码分割 (chunk-*.js)
- 登录模式: 用户名 + 密码 + TOTP (Google Authenticator)
- 路由模式: Hash路由 (#/route)

---

## 二、登录流程技术细节

### 1. 登录步骤

#### 步骤1: 打开登录页
```
URL: https://operator1.otgpaytest.com/#/login
等待元素加载时间: 3秒
```

#### 步骤2: 输入用户名
```
定位方式: By.NAME = "username"
备选定位: By.XPATH = "//input[@placeholder='Please enter username']"
操作: send_keys(username)
```

#### 步骤3: 输入密码
```
定位方式: By.NAME = "password"
备选定位: By.XPATH = "//input[@type='password']"
操作: send_keys(password)
```

#### 步骤4: 生成并输入TOTP验证码
```
定位方式: By.NAME = "mfaCode"
备选定位: By.XPATH = "//input[contains(@placeholder,'MFA')]"
TOTP生成算法:
  - 标准: RFC 6238 TOTP
  - 密钥格式: Base32编码
  - 时间步长: 30秒
  - 数字数: 6位
  - 哈希算法: HMAC-SHA1

Python示例(使用pyotp库):
  import pyotp
  totp = pyotp.TOTP(secret_key)
  code = totp.now()  # 获取当前6位验证码

Python示例(原生实现):
  import base64, hashlib, hmac, struct, time
  def generate_totp(secret, interval=30, digits=6):
      normalized = secret.strip().replace(" ", "").upper()
      padded = normalized + "=" * (-len(normalized) % 8)
      key = base64.b32decode(padded, casefold=True)
      counter = int(time.time() // interval)
      msg = struct.pack(">Q", counter)
      digest = hmac.new(key, msg, hashlib.sha1).digest()
      offset = digest[-1] & 0x0F
      code = struct.unpack(">I", digest[offset:offset+4])[0] & 0x7FFFFFFF
      return str(code % (10**digits)).zfill(digits)
```

#### 步骤5: 点击登录按钮
```
定位方式: By.XPATH = "//button[normalize-space()='Login']"
等待条件: element_to_be_clickable
成功标志: 页面不再包含 "#/login"，OtgPay 元素出现
等待元素: (By.ID, "OtgPay")
```

### 2. 登录验证条件
```
- 登录成功: driver.current_url 不包含 "#/login"
- 主容器加载: document.getElementById("OtgPay") 存在并可见
- 侧边栏加载: .sidebar-container a[href^='#/'] 至少有一个链接
```

---

## 三、充值记录新增表单字段

### 1. 基本信息
```
页面URL: /operate/recharge/list
完整URL: https://operator1.otgpaytest.com/#/operate/recharge/list
打开弹窗方式: 点击列表中的 "充值" 按钮
弹窗类型: Element UI el-dialog
表单类型: Element UI el-form (v-model绑定)
```

### 2. 表单字段列表

| 字段名 | 类型 | 是否必填 | 说明 | 字段定位 |
|-------|------|--------|------|---------|
| 用户编码 | 级联下拉框 | 是 | 选择用户ID (如299) | get_form_item_by_label("用户编码") |
| 商户名称 | 级联下拉框 | 是 | 依赖用户编码，级联加载 | get_form_item_by_label("商户名称") |
| 充值金额 | 数值输入框 | 是 | 支持浮点数 | get_form_item_by_label("充值金额") |
| 收款地址 | 下拉选择 | 是 | 选择收款地址 | get_form_item_by_label("收款地址") |
| 谷歌验证码 | 文本输入 | 是 | TOTP 6位验证码 | get_form_item_by_label("谷歌验证码") |
| 商户凭证 | 特殊输入 | 否 | 点击按钮后在弹框输入 | get_form_item_by_label("商户凭证") |
| 备注 | 多行文本(textarea) | 否 | 备注说明信息 | get_form_item_by_label("备注") |

### 3. 表单操作特点
```
- 级联关系: 用户编码 → 商户名称 (后者需要等待前者加载)
- 延迟加载: 选择用户编码后需要等待 2-3 秒商户列表加载
- 下拉框类型: Element UI el-select (CSS定位: .el-select)
- 下拉选项定位: .el-select-dropdown__item
- 提交按钮文本: "保存"
- 提交成功提示: .el-message--success
- 表单验证: 弹窗内 .el-form-item__error 显示错误信息
```

---

## 四、提现记录新增表单字段

### 1. 基本信息
```
页面URL: /operate/withdraw/list
完整URL: https://operator1.otgpaytest.com/#/operate/withdraw/list
打开弹窗方式: 点击列表中的 "申请下发" 按钮
弹窗类型: Element UI el-dialog
```

### 2. 表单字段列表

| 字段名 | 类型 | 是否必填 | 说明 | 字段定位 |
|-------|------|--------|------|---------|
| 商户名称 | 下拉选择 | 是 | 选择商户(如ceshi8) | get_form_item_by_label("商户名称") |
| 提现金额 | 数值输入框 | 是 | 提现金额数值 | get_form_item_by_label("提现金额") |
| 收款地址 | 文本输入 | 是 | 区块链地址或银行信息 | get_form_item_by_label("收款地址") |
| 谷歌验证码 | 文本输入 | 是 | TOTP 6位验证码 | get_form_item_by_label("谷歌验证码") |

### 3. 表单操作特点
```
- 商户选择: 选择后可能出现新字段(级联关系)
- 提交按钮文本: "提交"
- 提交成功提示: .el-message--success
- 表单验证: 弹窗内 .el-form-item__error 显示必填提示
```

---

## 五、商户互转新增表单字段

### 1. 基本信息
```
页面URL: /operate/merchantTransfer/list
完整URL: https://operator1.otgpaytest.com/#/operate/merchantTransfer/list
我要转帐页面: /operate/transferApplication/index
完整URL: https://operator1.otgpaytest.com/#/operate/transferApplication/index
弹窗类型: Element UI el-dialog
```

### 2. 商户互转列表页面功能
```
- 筛选功能: 支持按互转流水搜索
- 状态筛选: 支持按状态下拉过滤
- 表格操作: 支持审核操作(审核按钮)
```

### 3. 审核表单字段

| 字段名 | 类型 | 是否必填 | 说明 |
|-------|------|--------|------|
| 审核状态 | 单选框(radio) | 是 | 通过/拒绝选项 |
| 谷歌验证码 | 文本输入 | 是 | TOTP 6位验证码 |
| 审核原因 | 多行文本(textarea) | 是 | 审核说明 |

### 4. 审核弹窗操作特点
```
- 打开方式: 点击表格中的 "审核" 按钮
- 提交按钮文本: "确定"
- 成功标志: .el-message--success 或弹窗关闭
- 单选框定位: .el-dialog .el-form-item 中的 .el-radio
```

---

## 六、语言切换技术细节

### 1. 切换到中文
```python
实现方式:
1. 定位语言按钮: 
   CSS选择器: .international .el-dropdown-selfdefine
              .international.right-menu-item
              .international-icon
   
2. 触发下拉菜单:
   方式A: ActionChains(driver).move_to_element(button).click()
   方式B: driver.execute_script("arguments[0].click()", button)
   
3. 等待菜单展开: time.sleep(0.5-1秒)

4. 定位中文选项:
   XPath: //li[contains(@class, 'el-dropdown-menu__item') 
              and contains(normalize-space(.), '中文')
              and not(contains(@class, 'is-disabled'))]
   
5. 点击中文选项: option.click() 或 driver.execute_script("arguments[0].click()", option)

6. 等待页面重载: time.sleep(2秒)
```

### 2. CSS选择器
```
语言按钮位置: 顶部导航栏右侧菜单
菜单样式类: .el-dropdown-menu__item
禁用样式: is-disabled
```

---

## 七、泰国/货币切换技术细节

### 1. 切换到泰国(泰铢)
```python
实现方式:
1. 定位货币选择框:
   CSS选择器: .right-menu .currency-select input
             .navbar .currency-select input
             .currency-select .el-input__inner
   
2. 点击打开下拉框:
   driver.execute_script("arguments[0].click()", currency_input)
   
3. 等待选项加载: time.sleep(0.5-1秒)

4. 定位泰国/泰铢选项:
   XPath: //li[contains(@class, 'el-select-dropdown__item')
              and not(contains(@class, 'is-disabled'))
              and (contains(normalize-space(.), 'Thailand') 
                   or contains(normalize-space(.), '泰铢'))]
   
5. 点击泰国选项: driver.execute_script("arguments[0].click()", option)

6. 等待切换完成: time.sleep(2秒)
```

### 2. 货币选择框位置
```
位置: 顶部导航栏右侧(导航栏 .navbar 或 .right-menu 中)
样式类: .currency-select
选项下拉: .el-select-dropdown__item
```

### 3. 支持的货币关键词
```
泰国: "Thailand" 或 "泰铢"
印尼: "印尼盾"
其他: 根据系统配置
```

---

## 八、系统架构特点

### 1. 前端技术栈
```
框架: Vue.js (可能版本2.x)
UI库: Element UI
路由: Hash路由 (#/路径)
CSS预处理: Webpack构建
代码分割: chunk-*.js 文件
```

### 2. 菜单导航结构
```
侧边栏类: .sidebar-container
菜单链接: a[href^='#/']
菜单项: .el-menu-item, .el-submenu__title
正在加载: 主容器 #OtgPay

系统菜单示例:
├── 财务管理 (第4个菜单)
│   ├── 充值记录 (第5个子菜单)
│   ├── 提现记录
│   ├── 商户互转
│   └── 我要转帐
└── 其他菜单...
```

### 3. 表单控件约定
```
表单容器: .el-form
表单项: .el-form-item
标签: .el-form-item__label
输入框: .el-input > input
下拉框: .el-select > .el-input__inner (readonly)
多行文本: textarea
单选框: .el-radio
错误提示: .el-form-item__error
```

### 4. 对话框约定
```
对话框容器: .el-dialog
对话框头: .el-dialog__header
关闭按钮: .el-dialog__headerbtn
按钮容器: .el-dialog .el-dialog__footer
取消按钮: button with text "取消"
```

---

## 九、Selenium定位策略总结

### 1. 推荐定位方式优先级
```
优先级1: 按 CSS_SELECTOR (.class, #id)
优先级2: 按 NAME (input name属性)
优先级3: 按 XPath (复杂定位)
优先级4: 按 ID
优先级5: 按 XPATH (最后手段，易失效)
```

### 2. 多定位器兜底模式
```python
# 提供多个定位器，按优先级尝试
locators = [
    (By.CSS_SELECTOR, "specific-selector"),
    (By.NAME, "input-name"),
    (By.XPATH, "//fallback/xpath"),
]

for by, value in locators:
    try:
        element = wait.until(EC.visibility_of_element_located((by, value)))
        return element
    except:
        continue
```

---

## 十、关键等待条件

### 1. 登录完成
```python
wait.until(lambda d: d.find_elements(By.ID, "OtgPay") or "#/login" not in d.current_url)
```

### 2. 菜单加载
```python
wait.until(EC.presence_of_element_located((By.CSS_SELECTOR, ".sidebar-container a[href^='#/']")))
```

### 3. 下拉框选项加载
```python
wait.until(EC.element_to_be_clickable((By.XPATH, "//li[contains(@class, 'el-select-dropdown__item')]")))
time.sleep(1-2)  # 级联加载需要额外等待
```

### 4. 提交成功
```python
# 方式1: 检查成功提示
success_elements = driver.find_elements(By.CSS_SELECTOR, ".el-message--success")

# 方式2: 检查弹窗关闭
dialog_present = any(d.is_displayed() for d in driver.find_elements(By.CSS_SELECTOR, ".el-dialog"))
```

---

## 十一、常见问题处理

### 1. 元素不可交互 (Element not clickable)
```python
# 解决方案1: JS点击
driver.execute_script("arguments[0].click();", element)

# 解决方案2: JS设置值
driver.execute_script("""
    var el = arguments[0];
    el.removeAttribute('readonly');
    el.removeAttribute('disabled');
    el.value = arguments[1];
    el.dispatchEvent(new Event('input', { bubbles: true }));
    el.dispatchEvent(new Event('change', { bubbles: true }));
""", element, value)

# 解决方案3: 滚动后点击
driver.execute_script("arguments[0].scrollIntoView({block: 'center'});", element)
time.sleep(0.2)
element.click()
```

### 2. 级联加载超时
```python
# 选择第一个下拉框后需要等待
select_el_dropdown(driver, item, option_text="299")
time.sleep(2-3)  # 等待级联加载

# 然后选择第二个下拉框
select_el_dropdown(driver, item, index=0)
```

### 3. 下拉菜单未显示
```python
# 关闭临时浮层
driver.execute_script("""
    document.querySelectorAll(
        '.el-select-dropdown, .el-picker-panel, .el-popover'
    ).forEach(el => {
        el.style.display = 'none';
    });
""")
```

---

## 十二、文件路径参考

### HTML文件
- `/Users/apple/Documents/ceshi/recharge_add_form.html` - 充值表单页面
- `/Users/apple/Documents/ceshi/withdrawals_add_form.html` - 提现表单页面
- `/Users/apple/Documents/ceshi/merchant_transfer_add_form.html` - 商户互转表单页面
- `/Users/apple/Documents/ceshi/recharge_page.html` - 充值列表页面
- `/Users/apple/Documents/ceshi/page_after_login_full.html` - 登录后首页

### Python脚本
- `/Users/apple/Documents/ceshi/zdh.py` - 基础登录与菜单点击脚本
- `/Users/apple/Documents/ceshi/zdhcs.py` - 日志完整的登录脚本
- `/Users/apple/Documents/ceshi/zdhcsCL.py` - 充值/提现/互转完整测试脚本
- `/Users/apple/Documents/ceshi/zdhcsGX.py` - 全模块遍历测试脚本
- `/Users/apple/Documents/ceshi/explore_system.py` - 系统探索脚本

### 截图文件
- `/Users/apple/Documents/ceshi/01_login_page.png` - 登录页
- `/Users/apple/Documents/ceshi/01_after_login.png` - 登录后
- `/Users/apple/Documents/ceshi/02_finance_menu.png` - 财务菜单
- `/Users/apple/Documents/ceshi/03_recharge_page.png` - 充值列表
- `/Users/apple/Documents/ceshi/04_recharge_add_form.png` - 充值表单
- 其他各版本探索截图...

---

## 总结

OTG 支付系统是一个基于 Vue.js + Element UI 的现代Web应用，采用Hash路由，支持TOTP双因素认证。系统的关键特点是：

1. **多层级认证**: 用户名 + 密码 + TOTP
2. **级联表单**: 表单字段间有依赖关系，需要分步处理
3. **Element UI库**: 统一的UI组件库，定位相对规范
4. **异步加载**: 下拉框选项、级联数据需要额外等待
5. **国际化支持**: 支持中文和泰国(泰铢)等多语言/多货币

自动化测试需要重点处理：
- TOTP动态验证码生成
- 级联下拉框的加载延迟
- Element UI组件的定位和交互
- 多定位器兜底策略
- 适当的等待和错误处理

